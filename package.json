{
  "name": "calendarmarkdown",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dist": "esbuild src/index.ts --bundle --minify --outfile=$APP_FOLDER_NAME/bundle.js --target=es2016",
    "build": "npm run dist && cp index.html styles.css $APP_FOLDER_NAME",
    "dev": "APP_FOLDER_NAME=dist npm run dist -- --serve=8080 --servedir=./dist",
    "test": "jest",
    "watch": "esbuild src/index.ts --bundle --outfile=dist/bundle.js --target=es2016 --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "esbuild": "^0.28.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "typescript": "^6.0.2"
  }
}